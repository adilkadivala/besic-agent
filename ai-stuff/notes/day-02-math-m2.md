# Math M2 — Functions (input → output)

**Status:** In progress  
**Goal:** See a model as a machine: put numbers in, get numbers out.

## Q&A

### Q1 — What is a function?
**Student answer:**
Function is a command which performs based on its instructions; some have no input (greeting) but produce output; some take input and produce output (sum, multiply).

**Teacher feedback:** 8/10 — strong programming intuition. For DL math: think "machine: input → output following a recipe." Zero-input funcs exist in code; neural nets almost always take inputs.

### Q2 — What is f(3) for f(x) = 5*x + 1?
**Student answer:**
16

**Teacher feedback:** 10/10 — 5*3+1 = 16

### Q3 — Name input and output for a model example
**Student answer (1st):** both are positive numbers
**Student answer (2nd):** input = prompt to the model; output = 7

**Teacher feedback:**
- Output idea good: model answers with the digit (e.g. 7)
- Input fix: for THIS app, input is the **photo/image**, not a text "prompt"
- "Prompt" is mainly for LLMs/chat models
- Score: 6.5/10 — close after rethink; wording precision needed
- Model answer: Input = photo of a handwritten digit. Output = which digit it is (0–9), e.g. 7.

### Q4 — LLM input/output
**Student answer:**
input is text (prompt), output also text

**Teacher feedback:** 10/10 — exact. Chat LLM: text in → text out.

## M2 complete ✓
